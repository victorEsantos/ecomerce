<div class="card">
    <div class="cardbody">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <small>data: {{postagem.data}}</small>
        <hr>

        <small>{{postagem.conteudo}}</small>
        <br>
        
    </div>
</div>
{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h3>Editar categoria</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/categorias/edit" method="POST">
            <input type="hidden" name="id" value="{{categoria._id}}">
            <label for="nome">Nome:</label>
            <input value="{{categoria.nome}}" type="text" name="nome" id="nome" placeholder="nome da categoria" class="form-control">
            
            <label for="slug">Slug:</label>
            <input value="{{categoria.slug}}" type="text" id="slug" name="slug" class="form-control">
            <br>
            <button type="submit" class="btn btn-success">Editar Categoria</button>
        </form>
    </div>
</div>